plugins {
    id 'org.spongepowered.gradle.vanilla' version '0.2.1-SNAPSHOT'
}

base.archivesName.set("${mod_id}-common")

minecraft.version(mc_version)

sourceSets.main.resources.srcDir 'src/generated/resources'